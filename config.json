{
	"nb_classes":2,
	"epochs":1000,
	"batch_size":350,
	"weights":"any name of the file with (.h5) extention",
	"pre_train":"False",
	"shuffle":"True",
	"dataset_name":"Directory name where the labeled pre-processed dataset is kept",
	"raw_path":"The path to the dataset",
	"train_test_split":0.8,
	"validation_split":0.2,
	"data_path":"Directory name where the labeled pre-processed dataset is kept"
}
